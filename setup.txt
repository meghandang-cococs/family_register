ssh gary@imacwin
cd Cococs/Python/FastAPI
source ~/Python/FastAPI/fastAPIenv/bin/activate
uvicorn app.main:app --host 0.0.0.0 --port 8080 --reload
Finder > Go > Connect to Server 
ps -ef | grep uvico
kill xxx
